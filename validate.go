package arbitrary

// Validate will walk the unstructured data to check that all leaf nodes are valid
// types. Nil return value means the structure is clean.
func Validate(data interface{}) error {
	return nil
}
